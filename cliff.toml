[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.\n
"""
body = """
{% if version -%}
## {{ version | trim_start_matches(pat="@headless-toast/") }} - {{ timestamp | date(format="%Y-%m-%d") }}
{% else -%}
## unreleased
{% endif -%}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits -%}
- {% if commit.scope %}*({{ commit.scope }})* {% endif %}
  {%- if commit.breaking %}[**breaking**] {% endif %}
  {{- commit.message | upper_first }}
{% endfor %}
{% endfor %}
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_parsers = [
    { footer = "(?i)skip-changelog:\\s*true", skip = true },
    { message = "^chore\\(release\\)!?:", skip = true },
    { message = "^chore\\(deps.*\\)!?:", skip = true },
    { message = "^feat", group = "<!-- 0 -->Features" },
    { message = "^fix", group = "<!-- 1 -->Bug Fixes" },
    { message = "^refactor", group = "<!-- 2 -->Refactoring" },
    { message = "^doc", group = "<!-- 3 -->Documentation" },
    { message = "^perf", group = "<!-- 4 -->Performance" },
    { message = "^style", group = "<!-- 5 -->Styling" },
    { message = "^test", group = "<!-- 6 -->Testing" },
    { body = ".*security", group = "<!-- 7 -->Security" },
    { message = "^chore|^ci", group = "<!-- 8 -->Miscellaneous" },
]
protect_breaking_commits = false
filter_commits = false
sort_commits = "newest"
